# Test Environment Configuration
# =============================
# This file contains all environment variables needed for running tests.
# It is committed to the repository since it contains only test values.
#
# In CI/CD, this file is copied to .env before running tests.
# Database connection values are overridden in CI to use the GitHub Actions PostgreSQL service.
#
# DO NOT put any real secrets or production values in this file!

# Test environment configuration
FASTAPI_ENV=TEST
CONFIG_NAME=TEST
VERSION=0.1.0-test
DEBUG=true

# Database (for CI, these will be overridden)
DB_HOST=localhost
DB_PORT=5432
DB_USER=testuser
DB_NAME=testdb
PGPASSWORD=testpass

# JWT Configuration
JWT_SECRET_KEY=test-secret-key-for-testing
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24
JWT_ACCESS_EXPIRATION_MINUTES=15
JWT_REFRESH_EXPIRATION_DAYS=7

# Agent/External API Configuration
agent_ws_base=ws://test.example.com
agent_url=/test/agent
agent_sql_url=/test/sql-agent
agent_base=http://test.example.com
agent_scenario_url=/test/scenario

# Asset API Configuration
api_base=http://api.test.com
api_asset_url=/assets
api_neighbor_url=/neighbors
api_name_url=/names
api_id_url=/ids

# Semantic Search Configuration
semantic_base=http://semantic.test.com
semantic_emb_url=/embedding
semantic_search_url=/search
semantic_rank_url=/ranking
semantic_table=test_table

# Email Configuration (using capitalized names per ruff linting)
SMTP_HOST=smtp.test.com
SMTP_PORT=587
SENDER_EMAIL=test@example.com
APP_PASSWORD=test-password

# Legacy email config (for backward compatibility)
smtp_host=smtp.test.com
smtp_port=587
sender_email=test@example.com
app_password=test-password

# Organization
organisation_name=Test Organization
